# SAGE  - Internet Guru who formed the first stack
# TWO Deep Directory File Reconstitution
# src has 100000 directories made by a commercial program
# to backup and categorized files into archaic number files, pics, etc.. but pulling them into
# directories of all the same time for consideration of merge
# into multiple back of files... this and a duplicate finder might be useful on due diligence. 
import os
from pathlib import Path



ext = ".pyd"
src = "D:\\Dropbox\\Sorting Hat\\$OF\\"
dst = "D:\\Dropbox\\Sorting Hat\\PYD\\"

p = Path(src)
for entry in p.iterdir():
    sp = Path(src+entry.name)
 
    if entry.is_dir():
        print ("Directory: ",entry.name)
        if not os.listdir(sp):
            print (f"{sp} IS AN EMPTY DIRECTORY")
            os.rmdir(sp)
            if os.path.isdir(sp):
                print ("Directory couldn't be removed")
            else:
                print ("Empty Directory Deleted")
            print ("\n\n")
            continue
        for sub_entry in sp.iterdir():
            print (sub_entry.name)
            if (sub_entry.name.endswith(ext)):
                x = sp / sub_entry.name
                y = dst + sub_entry.name


                if os.path.isfile(y):
                    i=0
                    print ("\n\nAttempt File exists already.")
                    z= y
                    while os.path.isfile(z):
                        k = str(i)
                        i += 1
                        j = str(i)
                        print (j, ": checking for ", z)
                        z= z.removesuffix(ext)
                        z= z.removesuffix(k)
                        z= z+j
                        z= z+ext
                    print ("change name plus ", j, " moving", x, z)
                    os.rename(x,z)
                    if os.path.isfile(z):
                        print ("File has been renamed and moved.")
                    else:
                        print ("Move Failed.")
                    print ("\n\n")

                    
                else:
                    print ("moving ", x, y)
                    os.rename(x,y)
                    if os.path.isfile(y):
                        print ("File has been moved.")
                    else:
                        print ("Move Failed.")
                    print ("\n\n")   
                
    else:
        print ("root dir:",entry.name)

print ("Suck It Complete")

